# Ekstraklasa-Racial

pip install -r requirements.txt

"python opta_scraper" (--headless | DEFAULT: false)

"python opta_formatter"(--data-in "PATH" | DEFAULT: data/optaformatter/result) (--data-out "PATH" | DEFAULT: data/optascraper/result)